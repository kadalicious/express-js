const gitignore = require('../assets/gitignore');
const packageJSON = require('../assets/package');
const app = require('../assets/app');
const route = require('../assets/route');
const AppError = require('../assets/AppError');
const errorHandler = require('../assets/errorHandler');
const writeToLog = require('../assets/writeToLog');


const folderNames = [
    ['public'],
    ['log'],
    ['src', [
        ['controllers'],
        ['models'],
        ['templates', [
            ['components'],
            ['views']]
        ],
        ['utils', [
            ['text:AppError.js', AppError],
            ['text:writeToLog.js', writeToLog],
            ['text:errorHandler.js', errorHandler]]
        ],
        ['routes', [
            ['text:helloRoute.js', route]]
        ]
    ]
    ],
    ['text:app.js', app],
    ['text:.gitignore', gitignore],
    ['text:package.json', packageJSON],
    ['text:.env', 'NODE_ENV=development']
];

module.exports = folderNames